<section class="page">
<div class="album__img"><%= attachment_image_tag @album, :jacket_image , :fill,250,250  %></div>
<div class="customer__wrapper">
  <div class="customer__tabs">
    <div class="customer__tab is_selected">商品情報</div>
    <div class="customer__tab">含む曲</div>
    <div class="customer__tab">含むアーティスト</div>
    
  </div>
    <%= render "admins/albums/album_infos" %>
    <%= render "admins/albums/album_songs" %>
    <%= render "admins/albums/album_singers" %>

</section>
<% if @album.stock_quantity ==0 %>

<% else%>

    <div class="btn__cart">
    <p class="btn btn__login"><%= link_to "カートに入れる" , album_cart_albums_path(@album), class:"btn btn__link", method: :post %></p>
    </div>
<% end %>
